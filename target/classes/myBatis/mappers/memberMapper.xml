<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="memberMapper">

	<insert id="join" parameterType="MemberDTO">
		insert into member values(#{id},#{pw},#{name},#{email},#{phone},#{address},#{gender},sysdate)
	</insert>
	
	<select id="login" parameterType="MemberDTO" resultType="MemberDTO">
		select * from member where id=#{id} and pw=#{pw}
	</select>
	
	<update id="modify" parameterType="MemberDTO">
		update member set pw=#{pw}, email=#{email}, phone=#{phone}, address=#{address} where id=#{id}
	</update>
	
	<delete id="delete" parameterType="java.lang.String">
		delete member where id=#{id}
	</delete>
	
	<select id="list" parameterType="Pager" resultType="MemberDTO">
		select * from(
		select rownum R, M.* from(
		select * from member order by id desc) M)
		where R between #{startRow} and #{lastRow}
	</select>
	
	<select id="totalCount" resultType="Integer">
		select count(id) from member
	</select>
		
</mapper>  